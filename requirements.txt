# Local development / Streamlit app dependencies.
# Installs PreflightOps in editable mode with the Streamlit UI and test extras.
# Package metadata and dependency versions live in pyproject.toml.
-e .[app,test]
